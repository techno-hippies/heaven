name = "vpn-status"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
ENVIRONMENT = "production"

# D1 database for storing DNS canary tokens
[[d1_databases]]
binding = "DB"
database_name = "heaven-vpn"
database_id = "" # Set after: wrangler d1 create heaven-vpn

# Routes for wildcard subdomain DNS canary
# routes = [
#   { pattern = "vpncheck.heaven.example/*", zone_name = "heaven.example" },
#   { pattern = "*.vpncheck.heaven.example/*", zone_name = "heaven.example" }
# ]
