{
  "name": "scrobble-log-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for ScrobbleLogV2 on Base Sepolia",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy scrobble-log --node https://api.studio.thegraph.com/deploy/",
    "create-local": "graph create --node http://localhost:8020/ scrobble-log",
    "remove-local": "graph remove --node http://localhost:8020/ scrobble-log",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 scrobble-log",
    "auth": "graph auth"
  },
  "keywords": [
    "the-graph",
    "base",
    "scrobble",
    "music"
  ],
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.98.1",
    "@graphprotocol/graph-ts": "^0.35.1"
  }
}
